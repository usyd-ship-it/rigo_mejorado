export default function Home() {
  return (
    <main style={{ fontFamily: "sans-serif", padding: "2rem", color: "#241A1E" }}>
      <h1 style={{ color: "#7A1F2B" }}>RIGO+ Mando</h1>
      <p>Scaffold B1. Dominio/sesión separados del portal público (spec punto 8). El UX real se migra desde <code>mando.html</code> (Pista A, congelado) en los siguientes hitos.</p>
    </main>
  );
}
